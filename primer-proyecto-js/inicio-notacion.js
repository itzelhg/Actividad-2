//Notacion
//; ----> Delimitar el termino de una linea, (no siempre se usa)
//Ejemplo: 
const a = 1;
a;
console.log(a);

//. ----> Acceder a los atributos de un objeto 
// Ejemplo:
alumno.edad;

//[] ----> square brackets 
//declarar listas, arrays o vectores
//Ejemplo de declaracion
const ar = [1,2,3,4];

//() ---> Parenthesis function
function promedio(a,b,c,d){
    //Toda la funcion
}
for(var a = 4, a > if, i++){
    //Bucle
}
if(let a, b, c, d){
    //Funcion
}
//{} ---> Curly Brackets
//Me permiten realizar objetos, funciones y estructuras de control
const alumnoo = {
    edad: 15,
}

for(var a = 4, a > if, i++){

}

if(let a, b, c, d){
    //Estructura de control
}else{
    //Funcions
}